[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q -m "not integration"
markers =
    integration: marks long-running or environment-dependent integration tests
    no_timeout: disable the per-test timeout guard for a specific test
